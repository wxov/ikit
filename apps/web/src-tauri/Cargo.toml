[package]
name = "ikit"
version = "0.1.2"
description = "i-kit 桌面端"
authors = ["i-kit"]
edition = "2021"

[lib]
name = "ikit_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
