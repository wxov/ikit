# i-kit 界面布局借鉴 参考站 与功能扩展需求文档

> 版本：v0.2（as-built）
> **本文档说明**：第 1–13 节为参考站分析与需求设计；**§14「现状总览」为当前实际实现记录（以代码为准）**。其他历史文档（PRD / 插件系统需求）中的现状说明同样以此为准。
> 范围：**只改布局 + 只增功能**；保留本项目全部既有功能（知识库 / AI Agent / 插件商店 / 系统 / 用户管理 / 数据统计 / 便签 / 日历 / 待办 / 天气 / RSS / Markdown 剪贴板等）。
> 前置约束（已落实）：登录改为「点击头像弹登录框」，注册已屏蔽（站主手动建号）。完成前不自动提交/部署。

---

## 1. 背景与目标

当前 i-kit 的前台是「控制台 / 面板」形态：顶部导航 + 主区切换 + 移动端底部 Tab。用户希望参考 **参考站**（，Typecho 主题风格的个人知识门户）的**三栏门户式布局**，把既有功能包裹成更接近「内容站点 / 知识门户」的体验，并借鉴其特色功能。

**坚持的原则**
- 既有功能一个都不删，只调整呈现方式与入口。
- 新增功能与布局改造分开推进，先文档、后分层实施。
- 站主（admin）功能与普通用户（前台）功能保持隔离。

---

## 2. 参考站 参考站 布局与功能分析

> 依据浏览器实测（截图 + DOM）。参考站 是 Typecho 博客主题，核心形态如下。

### 2.1 整体结构：三栏门户
```
┌─────────────────────────────────────────────────────────┐
│ Header：Logo + 站名 | 快捷入口(小店/FRP/RSS/TG群/仓库)     │
│          | 全局搜索(Ctrl+K) | 右上角头像                   │
├──────────┬────────────────────────────┬──────────────────┤
│ 左侧导航  │     主内容（卡片流 feed）      │   右侧 widget 列  │
│ - 站签名   │  - 封面大图卡片，标题叠加     │  - 热门文章       │
│ - 分类树   │  - 摘要 / 作者/时间/浏览/标签 │  - 最新评论       │
│ - 专栏/专题 │  - 分页                    │  - 动态日历       │
│ - 页面链接  │                            │  - 统计图表(雷达/柱状)│
│ - 快捷链接  │                            │  - 标签云         │
└──────────┴────────────────────────────┴──────────────────┘
                        页脚
```

### 2.2 关键布局特征
1. **顶栏**：左 Logo+站名；中间一组快速功能入口（小店/FRP/RSS/TG群/仓库 等外链按钮）；**全局搜索**（占位「搜索（Ctrl + K）」）；极右**头像**（本布局用于登录/个人中心）。
2. **左栏**：竖向导航。顶部一句博主签名 moto；下方是**多级分类树**（如 分类 → 日常资讯/数字生活/运维部署/技术教程/群晖专栏，专栏再分 基础/网络/套件/高级）。再往下是「页面」组（留言板 / 归档库 / 关于我）和「快捷入口」（小店/FRP/RSS/仓库）。
3. **中栏**：**卡片流**。每条卡片 = 封面大图（标题压图、渐变遮罩）+ 正文标题 + 摘要 + 元信息（作者 / 时间 / 浏览数 / 标签 / 分类）。鼠标悬停有位移/阴影动效；底部为分页。
4. **右栏**：**widget 堆叠**。热门文章、最新评论、动态日历、博主发布/评论月统计、**分类雷达图**、**发布统计图**、标签云。整体可折叠。
5. **响应式**：窄屏时左/右栏收起为抽屉，主内容单列。

### 2.3 可借鉴的「功能点」
来自实测提取：
- 多级分类导航（分类树、专栏/专题分组）
- 卡片式内容流（封面 + 标题压图 + 摘要 + 元信息）
- 全局搜索（支持 Ctrl+K 呼出）
- 阅读/浏览计数（"N 次浏览"）
- 热门文章 widget、最新评论 widget、标签云
- 动态日历 widget（近月发布热点日历）
- 统计图表 widget（分类雷达图、发布柱状图、近 10 月发布/评论统计）
- 「专栏/专题」策划（类似知识库的专题合集）
- 页面链接：留言板、归档库、关于我
- 站签名 moto、博主形象位（左栏头像/签名）
- 分页、文章作者/时间/标签元信息排版

---

## 3. 现状盘点（i-kit 已有）

| 模块 | 现状 | 归属 |
|---|---|---|
| 知识库 | CRUD / 混合检索 / 分类树 / 回收站 / 置顶 / 导出 / 版本 / AI 摘要 / 评分 / 点赞 / 分享 / 状态 | 功能插件 `knowledge` |
| AI Agent | 工具 + RAG + SSE 流式对话 | 功能插件 `agent` |
| 插件商店 | 浏览/安装/更新/卸载/评分/评论 | 站主 `store` |
| 插件管理 | 启用/停用/配置 | 站主 `manager` |
| 系统 | 系统信息/更新/健康 | 站主 `system` |
| 用户管理 | 列表/角色/重置/禁用（**尚无站主手动加号**） | 站主 `users` |
| 数据统计 | 汇总计数 | 功能插件（商店）`stats` |
| 便签 / 日历 / 待办 / 天气 / RSS / MD剪贴板 | 独立功能面板 | 功能插件（商店） |
| 登录/注册 | AuthPanel（登录+注册+找回） | 全局 |

**当前布局**：单行顶栏（仅 admin 有「管理」下拉分组）+ 主区切换 + 移动端底部 Tab。非站主默认落第一个功能插件。

---

## 4. 总体改造方向

把「前台功能区」从「控制台」升级为「**知识门户**」三栏布局，站主管理收敛到独立的后台/分组；登录走头像弹框。

```
新前台（普通用户 / 站主皆可见）
├── 顶栏：Logo+站名 | 快捷入口 | 全局搜索(Ctrl+K) | 头像(登录弹框) | 暗色切换
├── 左栏：我的/知识库/Agent/… 功能插件导航（分类树化） + 页面(归档/关于/留言)
├── 中栏：知识卡片流（默认 tab）+ 具体功能面板（Agent→对话流、统计→卡片、便签…）
└── 右栏：热门文章 / 最新评论 / 标签云 / 动态日历 / 统计图表（widget 化）

站主后台（仅 admin，独立分区）
├── 系统 / 插件管理 / 插件商店 / 用户管理（保留现有，入口收敛）
```

**关键点**：功能插件（knowledge/agent/stats/notes/…）仍是 `tab` 切换的主体；「知识库内容」以 参考站 卡片流当作中栏**默认首屏**；RSS/天气/日历等面板可同时作为右栏 widget 复用来丰富门户。

---

## 5. 布局改造需求

### 5.1 顶栏 Header
- 左侧：Logo + 站名「i-kit 控制台」→ 可预留「i-kit」品牌位 + 标语位。
- 中间：快捷功能入口（原有功能插件可平铺，或以「更多」收敛）——对应 参考站 的小店/FRP/RSS/TG群 位。
- 全局搜索框（占位「搜索（Ctrl + K）」）：回车弹出/聚焦后触发**知识库检索**，命中卡片流。
- 极右：**头像**（登录后显示头像+用户名缩略，点击弹出登录/账号菜单）→ 点击弹出**登录弹框**；未登录时为占位头像（也点开登录框）。暗色切换按钮保留。
- 移除原来顶栏直接堆叠的「system/manager/store/users」管理按钮（已收敛为「管理」分组，且仅在 admin 显示）。

### 5.2 左侧导航栏（桌面）
- 顶部：博主/站主签名 moto 位（可放一句话或站主昵称）。
- 分类/功能树：
  - 「知识库」展开 = 知识分类树（`CategoryTree`），点击分类 → 中栏按分类过滤卡片流。
  - 功能插件（Agent / 数据统计 / 便签 / 日历 / 待办 / 天气 / RSS / MD 剪贴板 …）作为同级导航项。
  - 「专栏/专题」：把部分内容策划成专题合集（可选，二期）。
- 「页面」组：归档库、关于我、留言板（见功能新增）。
- 站主专用（仅 admin）：系统 / 插件管理 / 插件商店 / 用户管理，位于底部，与普通功能**明显分隔**。

### 5.3 主内容区
- **默认首屏**：知识卡片流（复用 参考站 卡片：封面图 + 标题压图 + 摘要 + 作者/时间/浏览/标签/分类 + 分页）。
- 其它 tab：保留现有功能面板（Agent 对话流、统计信息、便签、日历、待办、天气、RSS、MD 剪贴板）。
- 顶部保留面包屑/当前分类标题 + 排序（最新/最热）+ 搜索过滤状态。

### 5.4 右侧 widget 栏（桌面）
- 热门文章（按浏览量/评分排序，点击定位卡片）
- 最新评论（知识库条目最新评分/评论）
- 标签云
- 动态日历（近 N 月有更新的日期高亮）
- 统计图表：分类雷达图 / 发布柱状图 / 近 10 月发布-评论趋势

### 5.5 响应式
- ≤768px：左右栏收起为抽屉/底部 Tab，主内容单列（保留现有底部 Tab 逻辑，但按新导航分组）。

---

## 6. 功能新增/增强需求

| 编号 | 功能 | 说明 | 数据来源 | 优先级 |
|---|---|---|---|---|
| N1 | 全局搜索（Ctrl+K） | 呼出搜索框，检索知识库标题/标签/正文，结果进卡片流 | `/api/knowledge` 检索接口 | P0 |
| N2 | 知识卡片流 | 封面图 + 标题压图 + 摘要 + 作者/时间/浏览/标签/分类 + 分页 | 知识库条目 | P0 |
| N3 | 左栏分类树导航 | 复用 `CategoryTree`，点击分类过滤卡片流 | 知识分类 | P0 |
| N4 | 头像登录弹框 | 点击头像弹登录框；未登录占位头像同样弹登录 | 账号 `/api/auth` | P0 |
| N5 | 屏蔽注册 | AuthPanel 隐藏注册 tab；站主在用户管理手动加号 | 用户管理 | P0 |
| N6 | 站主手动创建用户 | UserManager 增「添加用户」表单（用户名/密码/角色/昵称） | `/api/auth/admin/users` | P0 |
| N7 | 热门文章 widget | 按浏览量/评分排序 Top N | 知识库 | P1 |
| N8 | 最新评论 widget | 知识库最新评分/评论 | `/api/plugin-store` 评论/知识库评分 | P1 |
| N9 | 标签云 widget | 标签计数权重化展示 | 知识库标签 | P1 |
| N10 | 浏览计数 | 知识条目访问 +1 并展示 | 知识库 | P1 |
| N11 | 动态日历 widget | 近月有更新的日期高亮 | 知识库更新时间 | P1 |
| N12 | 统计图表 widget | 分类雷达/发布柱状/近 10 月趋势（复用 `stats` 插件） | `/api/plugins-data/statistics` | P1 |
| N13 | 关于我 / 留言板 / 归档库 页面 | 站点级信息页（归档按年月分组，留言板可复用便签/评论能力） | 站点配置 + 知识库 | P2 |
| N14 | 评分/点赞/分享入口 | 卡片或详情页常驻（已有能力，提升曝光） | 知识库 | P2 |
| N15 | 快捷功能入口 | 顶栏中部快捷位（RSS/天气/待办等一键可达） | 功能插件 | P2 |

> P0 = 首期必须；P1 = 门户体验增强；P2 = 可选。

---

## 7. 登录与账号调整

- **入口**：点击顶栏**头像** → 弹出登录框（复用 `AuthPanel` 视觉，改成弹窗而非整页/整栏）。
- **注册**：AuthPanel **隐藏注册 tab**，保留 登录 / 找回密码 两个入口。
- **新增用户方式**：站主登入后到「用户管理」→「添加用户」，填 用户名/密码/昵称/角色 → 由后端创建。后端 `register` 接口可保留但前端不暴露；或后端加 `adminCreateUser` 受 `requireAdmin` 保护。
- **登录态呈现**：头像 + 用户名 + 角色徽标；下拉菜单含「退出登录」。

---

## 8. 保留不变项

以下既有能力**不改语义、仅换入口/布局**：
- 知识库：CRUD、混合检索、分类、回收站、置顶、导出、版本、AI 摘要、评分、点赞、分享、状态。
- AI Agent：function-calling + RAG + SSE 流式。
- 插件商店 / 插件管理（启用/停用/配置）+ 安装/卸载/更新后自动刷新。
- 系统监控、用户管理（角色/重置/禁用）、数据统计。
- 便签 / 日历 / 待办 / 天气 / RSS / MD 剪贴板功能面板。
- 暗色模式、三端（Web / Tauri / Capacitor）兼容。

---

## 9. 功能拆解与优先级汇总

```
阶段一（骨架 + 登录改造）      P0
  ├─ 三栏门户壳：顶栏 / 左栏导航 / 中栏 / 右栏 widget 容器
  ├─ 头像登录弹框（替换现登录入口）
  ├─ 屏蔽注册（AuthPanel 隐藏注册 tab）
  ├─ 站主「用户管理」手动添加用户
  └─ 知识卡片流替换 knowledge 主视图（封面图 + 标题压图 + 摘要 + 元信息 + 分页）

阶段二（门户 widget 层）       P1
  ├─ 全局搜索（Ctrl+K）+ 检索结果进卡片流
  ├─ 左栏分类树导航（过滤卡片流）
  ├─ 热门文章 / 最新评论 / 标签云 widget
  ├─ 浏览计数
  └─ 动态日历 / 统计图表 widget

阶段三（内容型扩展）           P2
  ├─ 关于我 / 归档库 / 留言板 页面
  ├─ 评分/点赞/分享入口常驻
  └─ 快捷功能入口位
```

---

## 10. 数据与接口影响

- 知识库：增加/复用 **浏览计数** 字段与接口（`GET /api/knowledge/:id` +1 浏览，或独立 `POST /api/knowledge/:id/view`）。
- 统计 widget：复用 `/api/plugins-data/statistics`；若需分类雷达/发布柱状，扩展该统计接口返回分类分布与按月发布数。
- 用户：新增 `adminCreateUser` 受 `requireAdmin` 保护；前端隐藏 register。
- 布局数据：站点 moto / 站名 / 关于我 文案 → 站点配置项（可在系统设置中维护，或抽 `config` 服务）。
- 评论：知识库评分/评论与插件商店评论复用现有能力。

---

## 11. 实施阶段建议

1. **阶段一优先**：先落地三栏骨架 + 卡片流 + 头像登录 + 屏蔽注册 + 手动加用户。
2. 每阶段完成即：`pnpm --filter @ikit/web build` + `tsc --noEmit` + 后端接口冒烟，再进入下一阶段。
3. 布局组件化：新增 `PortalLayout`（三栏）、`NavTree`（左栏）、`Widget*`（右栏）、`ContentCardFeed`（中栏卡片流）；复用现有 `*Panel` 组件。
4. 完成后按 AGENTS.md 不自动提交/部署，等待用户确认。

---

## 12. 验收标准（阶段一）

- [ ] 桌面三栏门户布局生效（左导航/中卡片流/右 widget），窄屏退化为单列+底部 Tab。
- [ ] 顶栏含 Logo/站名、快捷入口、全局搜索、头像（点击弹登录框）、暗色切换。
- [ ] 头像未登录占位 + 登录后显示用户信息；点击均弹出登录框。
- [ ] AuthPanel 无注册入口（仅登录/找回）。
- [ ] 站主可在用户管理添加用户（用户名/密码/角色/昵称）。
- [ ] 知识库首屏为卡片流：封面图+标题压图+摘要+作者/时间/浏览/标签/分类+分页。
- [ ] 既有功能插件（Agent/统计/便签/日历/待办/天气/RSS/MD）经左栏仍可进入。
- [ ] 站主管理（系统/插件/商店/用户）仅 admin 可见且与前台功能分隔。
- [ ] 构建 + 类型检查通过；后端接口冒烟通过（登录、用户新增、知识检索、统计）。

---

## 13. 风险与注意事项

1. **既有功能不可回归**：改造是「换壳」，功能面板逻辑尽量不动，只调容器与路由。
2. **卡片流数据**：知识库条目未必都有封面图 → 需提供默认占位/渐变色封面。
3. **widget 复用**：日历 widget 可复用商店日历插件的视图组件；避免与主面板重复实现。
4. **三端一致性**：Tauri/Capacitor 下的响应式需一并验证（窄屏抽屉/底部 Tab）。
5. **站主隔离**：任何 new admin 功能都要 `requireAdmin`，前端仅在 admin 渲染。
6. **权限**：屏蔽注册后，新用户只能由站主创建，注意默认角色与密码策略。

---

## 14. 实施完成记录（截至当前版本）

> 本文档对应的实现已落地，与本项目既有功能兼容。以下为实施完成情况（与第 2/4/6 节对应）。

### 14.1 已实现
- **三栏门户**（`App.vue`）：顶栏（品牌 + 全局搜索 Ctrl+K + 站主管理(仅 admin) + 头像登录 + 暗色切换）、左栏（首页/新建文章(editor)/目录树/功能插件）、中栏（文章卡片流 或 文章详情）、右栏（数据概览/热门文章/最新评论/标签云）。
- **默认功能**：知识库已从插件库剥离（不再出现在插件管理/商店），作为门户默认功能呈现；`KnowledgePanel.vue` 旧编辑器已废弃（删除），编辑走全新 `ArticleEditor.vue`（仅站主）。
- **文章阅读**：点击卡片/目录条目 → 中栏直接显示 `ArticleDetail`（标题/元信息/浏览/点赞/星级/AI 摘要/Markdown 正文/评论/返回列表）；「首页」回到卡片流。
- **目录树（左栏，仅站主可管理）**：`DocTree.vue` + `DocTreeItem.vue` 层级目录；站主模式下支持 **拖拽排序/移动**（顶部/底部=同级排序，中部=移为子文档，含防循环）、`⋯` 菜单（新建子文档/移动到…/编辑/置顶/删除）、移动到点选模式。
- **归档 / 回收站（左栏）**：`ArchiveView.vue`（按年月分组，点击打开文章）、`TrashView.vue`（恢复/彻底删除/清空，管理操作仅站主；复用既有 `trash` 接口）。
- **封面图上传（站主）**：`POST /api/upload`（requireAdmin，base64 图片 ≤5MB）+ `@fastify/static` 托管 `/uploads/<file>`（`uploadsDir` 由 server 传入 `./data/uploads`）；`ArticleEditor.vue` 提供「📤 上传图片」按钮（选图 → 上传 → 回填封面 URL + 预览），卡片流/详情优先显示封面图。
- **批量管理（仅站主）**：卡片流 `☑ 批量管理` → 勾选多篇 → 一键修改分类 / 新增分类（`/api/knowledge/batch-category`、`POST /api/knowledge/categories`）。
- **登录与账号**：点头像弹登录框；注册已屏蔽（仅登录/找回）；站主在用户管理手动添加用户（`POST /api/auth/users`，requireAdmin）。
- **权限**：文章编辑（新建/编辑/删除/导入）后端为 `requireAdmin`；评论/评分/点赞/浏览对登录用户开放。
- **SQLite 持久化**：`entries` 表补齐 `cover/views/rating/likes/status/history/summary/share_token` 列（含迁移）；新增 `comments` 表（真实评论系统）。
- **右栏合并**：热门文章与最近访问合并为一个「热门文章」widget；右栏新增「分类」（CategoryTree）与「标签云」，点击分类/标签即**全局筛选**：中栏卡片流与左栏目录同步过滤（带「✕ 清除」筛选 chip，首页重置筛选）。
- **布局修复**：中间栏固定宽度不随内容变化；左右栏 sticky 固定、超视口内部滚动；三栏均为面板卡片背景（左/中/右）。

### 14.2 关联影响与约定
- 复用既有后端接口：`move`、`reorder`、`toggle-pin`、`batch-category`、`categories`（增删改）、`comments`、`auth/users`、`plugins-data/*`，无契约变更。
- 数据模型：`KnowledgeEntry` 增加 `cover/views/rating/likes/status/history/summary/shareToken/comments`（SQLite 已迁移）。
- 文档树层级由 `parentId`/`sortOrder` 决定；拖拽会修改真实数据（站主操作）。
- 废弃代码：`KnowledgePanel.vue`、`CategoryTree.vue` 已移除（被门户/目录树取代）。

### 14.3 待办 / 可选后续
- 封面图上传已完成；可选继续：图片裁剪/压缩、上传目录清理策略。
- 站主管理（系统/插件/商店/用户）位于底部栏（admin）；可选继续精简。
- 三端（Tauri/Capacitor）响应式与原生热更新验证（待进行：原生端构建 + 移动端媒体查询回归）。

---

## 15. 近期交互调整（2026-09，均已实现并验证）

- **布局骨架**：顶栏（品牌 + 全局搜索 Ctrl+K + 功能插件 + ws + 头像 + 暗色）｜三栏（左：首页/归档/回收站/新建文章 + 目录树；中：卡片流/文章详情；右：数据概览/分类/标签云/热门文章/最新评论）｜底部栏（品牌 + 站主管理(系统/插件/商店/用户，仅 admin) + 版权），左右与侧栏等宽、固定底部；侧栏底部与底栏顶部对齐（`max-height: calc(100vh - 128px)`，portal 底部内边距 = 底栏高 52px）。
- **站主管理移至底部栏**：功能插件移至顶部导航；左栏仅保留 首页/归档/回收站/新建文章 + 目录。
- **目录树**：左栏目录按 `parentId/sortOrder` 层级渲染；站主支持拖拽排序/移动、`⋯` 菜单（新建子文档/移动到…/编辑/置顶/删除）；目录面板 flex 填满左栏剩余高度、内部滚动。
- **卡片流**：参考站样式（通栏封面 → 标题 → 摘要 → 底部浅灰信息条：分类/日期/浏览/点赞/星级）；封面与卡片同宽齐顶；封面上传（`POST /api/upload` + `/uploads` 静态，站主）。
- **筛选联动**：右栏「分类」「标签云」点击 → 全局筛选（中栏卡片流 + 左栏目录同步过滤，带「✕ 清除」chip，首页重置）。
- **底栏/侧栏排版**：底部栏固定底部、与侧栏左右对齐；左右侧栏高度延伸至底栏顶部；统一滚动条（window/侧栏/目录均为 thin + 灰色圆角滑块 + 透明轨道、`overscroll-behavior: contain`、`scrollbar-gutter: stable`）。
- **权限**：文章新建/编辑/删除/导入、封面上传、批量分类、分类增删改、用户管理均为 `requireAdmin`；评论/评分/点赞/浏览对登录用户开放；注册屏蔽（站主手动建号）。
- **数据**：SQLite `entries` 全字段持久化（含 cover/views/rating/likes/status/history/summary/share_token）+ `comments` 表；JSON 兼容。

---

## 16. 代码评审与清理（2026-09）

- **安全修复**：移除 `resolveRole` 的 `?as=/x-role` 无会话提权回退（仅认真实 token）；`/api/plugins/export|import` 补 `requireAdmin`；`/api/auth/register` 删除（注册屏蔽，站主手动建号）；回收站 `restore` 包 `requireAdmin`；文章 `move/reorder/toggle-pin` 统一 `requireAdmin`（编辑类仅站主）。
- **删除 18 条前端无消费的死路由**（`register`、`entries/:id`、`import`、`children`、`status`、版本`restore`、`summary`、`rate/like/share`、`/api/share/:token`、`search` 路由、`category-rename`、`DELETE categories`、`plugins export/import`、商店评论删除、`agent/chat`）及连带死服务（knowledge 12 个方法、registry 3 个方法、agent `run/unregisterTool`、`JsonStore.data`、`SummarizeFn`）。
- **冗余/占位清理**：store-data `statistics()` 移除 `confCount` 占位与「健康状态」硬编码；loader 移除从不调用的 `dispose` 字段；`resolveRole` 复用 `tokenOf`；core 修剪死 re-export（仅保留 `ChatMessage/KnowledgeEntryInput`）；server `meta.plugins` 补全 7 个插件且版本取 `APP_VERSION`；`AuthPanel` 改用 `setToken()`；前端清理死状态/死样式/重复规则（App.vue/组件/lib/全局 style.css）。
- **`as any` 收敛**：`knowledge.listAllComments`、评论事件（扩展 `knowledge:changed` Events union：新增 `comment/comment-removed`）。
- **Bug 修复**：非 admin 用户现可删除自己的评论（注入 `username` 判断作者）。
- **深色模式**：调整为参考站式中性近黑（`--bg #151515 / --panel #1f1f1f / --border #333 / --text #e8e8e8` + 蓝强调）。
- 构建：`@ikit/web` / `@ikit/api` / `@ikit/core` / 各插件包 build 均通过，`tsc --noEmit` PASS；接口冒烟（存活 200、删除 404）通过。
