# i-kit 需求功能总览

> **文档性质**：本文是 i-kit 项目的**现状事实基准（主文档 / 总索引）**，由代码与既有文档盘点后整理而成。新增或修改需求时，**先更新本文**，再同步对应的 `docs/` 原文档（遵循 `AGENTS.md` 的"文档同步"要求）。
>
> - **版本**：v1.1（第 1 轮验收回写）
> - **更新日期**：2026-09
> - **维护说明**：本文不替代各原文档，而是以"摘要 + 指引 + 一致性标注"的方式引用它们；如本文与原文档或代码冲突，**以代码为准**，并回改本文。
> - **阅读顺序**：先读"一、文档索引"，再按"三、功能模块"定位到具体模块，最后看"四~七"的状态与规划。

---

## 一、文档索引与演进时间线

### 1.1 演进时间线（需求文档的版本关系）

```
个人知识库系统 PRD（V1.0 个人版）          ← 历史背景（仅个人版愿景，大量未落地）
        │  2026-08-29
        ▼
插件系统与权限管理体系扩展需求文档          ← 历史背景（插件化 + 三角色早期设计）
        │
        ▼
门户布局与功能改造需求文档（v0.2 as-built）  ← 当前门户形态的需求 + 实现记录（§14/15/16）
        │
        ├─► 用户组与内容可见性权限需求文档（v1.0 已实施）  ← 权限体系落地
        │
        └─► AI Agent 多节点与三端互通需求文档（v0.5）      ← Agent 多节点/任务落地
```

> 说明：前两份（PRD V1.0、插件系统与权限扩展）文档头部均已自我声明"仅作历史需求背景 / 设计背景参考"；实际现状以《门户布局与功能改造需求文档》§14 及本总览为准。

### 1.2 `docs/` 文件定位表

| 文件 | 定位 | 状态 |
|---|---|---|
| `docs\需求功能总览.md` | 本文：现状事实基准 + 总索引 | **有效（主文档）** |
| `docs\门户布局与功能改造需求文档.md` | 门户三栏改造需求 + as-built 实现记录（§14/15/16） | **有效（现状主要依据）** |
| `docs\用户组与内容可见性权限需求文档.md` | 用户组 + 内容可见性权限体系需求（已实施） | **有效（已实施）** |
| `docs\AI Agent 多节点与三端互通需求文档.md` | Agent 多会话/工具/节点/远程任务需求（阶段一~四完成） | **有效（阶段一~四完成，computer-use 暂缓）** |
| `docs\个人知识库系统 产品需求文档（PRD）.md` | 早期个人版 PRD（V1.0） | **历史参考（仅背景）** |
| `docs\插件系统与权限管理体系扩展需求文档.md` | 早期插件化 + 三角色设计 | **历史参考（仅背景）** |
| `docs\desktop-mobile.md` | 三端（Web/桌面/移动）构建指南 | **有效（部署/构建）** |
| `docs\server-deploy.md` | 服务器 Docker 部署指南 | **有效（部署）** |
| `docs\deepseek-harness AI Agent 交付团队配置.md` | DSH 交付团队编排配置（非产品需求） | **有效（团队/工具配置）** |
| 根 `README.md` | 项目总览 / 快速开始 / API 一览 | **有效（2026-09 已重写目录结构与 API 表）** |
| 根 `AGENTS.md` | 开发规范（分支/关联影响/数据模型） | **有效（数据模型与代码一致）** |

---

## 二、系统总览

### 2.1 一句话技术栈

机器人 + Web 服务 + AI Agent 成套项目：**Cordis 3 插件后端 + Fastify 5 API（REST + WebSocket + SSE）+ Vue 3/Vite 前端**，存储为 JSON（默认）/ SQLite（大数据量切换），部署为 Docker + docker-compose。

### 2.2 模块图

```
┌─────────────────────────────────────────────────────────────┐
│  前端 apps/web（Vue3，三端共用：Web / Tauri 桌面 / Capacitor 移动）│
│  App.vue 门户壳 + components/*（卡片流/目录树/编辑器/评论/Agent/…）  │
└──────────────┬──────────────────────────────────────────────┘
               │ REST /api/*  +  WebSocket /ws  +  SSE
┌──────────────▼──────────────────────────────────────────────┐
│  packages/api（Fastify：路由 + 鉴权 requireAdmin/requireUser + 事件桥）│
└──────────────┬──────────────────────────────────────────────┘
               │ ctx.set 服务调用
┌──────────────▼──────────────────────────────────────────────┐
│  packages/core（Cordis Context 装配 7 内置插件）               │
│  llm · demo · knowledge · agent · plugin-registry · account · store-data │
│  （+ plugin-registry 动态加载 plugins/* 第三方包，如 hello）     │
└──────────────┬──────────────────────────────────────────────┘
               │ 存储
        ┌──────┴───────┐
        │ JSON（默认）  │  SQLite（KB_STORAGE=sqlite，自动迁移）
        └──────────────┘
```

### 2.3 三端关系（摘要，详见 `docs\desktop-mobile.md`）

- 三端共用 `apps\web` 同一套前端 + 同一套 REST/WS 后端；唯一差异是 API 基地址（`apps\web\src\lib\config.ts` 读 `VITE_API_BASE`）。
- Web 端：相对路径（dev 走 Vite proxy，生产同源由 `@fastify/static` 托管 dist）。
- 桌面端：Tauri 2（`apps\web\src-tauri`），构建注入 `VITE_API_BASE=http://localhost:3000`；内含本地 agent 节点。
- 移动端：Capacitor（`capacitor.config.ts`），构建注入局域网 IP。
- WebSocket `/ws` 事件（如 `knowledge:changed`）让三端实时联动。

### 2.4 内置插件清单（`packages\core\src\index.ts:57-71` 装配顺序）

| 插件 | 服务 | 说明 |
|---|---|---|
| `llm` | `ctx.llm` | chat / chatStream（含 tools）/ embed |
| `demo` | `ctx.demo` | 最小示例 + `demo:greeted` 事件 |
| `knowledge` | `ctx.knowledge` | 知识库（CRUD/混合检索/目录/评论/可见性） |
| `agent` | `ctx.agent` | function-calling 运行时 + 会话/节点/任务 |
| `plugin-registry` | `ctx.pluginRegistry` | 插件注册表 + 商店 + 第三方包加载 |
| `account` | `ctx.account` | 账号 + 用户组 + 会话 |
| `store-data` | `ctx.storeData` | 商店插件真实数据（便签/日程/待办/天气/RSS/MD） |

> 另有第三方插件包 `plugins\hello\`（`index.ts` + `plugin.json`），由 `plugin-registry` 的 loader 动态加载，验证第三方插件链路。

---

## 三、功能模块

> 一致性状态标注含义：**A**=文档已写、代码已实现且一致；**B**=文档已写、代码未实现；**C**=代码已实现、文档未记录；**D**=文档与代码不一致/过时。

---

### 模块 ① 知识库内容管理（文章 / 目录树 / 分类 / 回收站 / 评论 / 封面 / 浏览计数）

**概述**：知识库是门户的**默认首屏（非插件）**。站主（admin）维护文章，游客按可见性只读，登录用户可评论。数据模型见 `packages\plugins\knowledge\src\types.ts`（`KnowledgeEntry` 字段 `:20-59`）。

**关键需求点（来源文档）**：
- 文章 CRUD + Markdown 渲染 + 代码高亮（`docs\个人知识库系统 PRD.md` §二；`docs\门户布局与功能改造需求文档.md` §8）。
- 目录树层级（`parentId`/`sortOrder`）、拖拽排序/移动、`⋯` 菜单（编辑/置顶/🔒权限设置/删除）、移动到点选（门户文档 §14）。
- 分类路径树（`技术/前端`）、批量归类、新增分类（PRD §二；门户文档 §14）。
- 回收站：软删除（`deletedAt`）→ 恢复 / 彻底删除 / 清空（PRD §二；门户文档 §14）。
- 评论：发表 / 删除（作者本人或 admin）；全站最新评论（门户文档 §14；**C：无专门需求文档**）。
- 封面图上传（站主，base64 ≤5MB，`/uploads/<file>` 静态托管）（门户文档 §14）。
- 浏览计数（访问 +1，公开接口，不可见文章不计数）（门户文档 §6 N10）。
- 文章可见性：四级（public/login/groups/private）+ 父子递归同步（self/same/all）+ 批量设置（`docs\用户组与内容可见性权限需求文档.md` §4）。

**实现位置**：
- 服务：`packages\plugins\knowledge\src\service.ts`（`isVisible` `:45-53`、`create` `:164-204`、`search` `:381-429`、`setVisibility` `:471-504`、评论 `:521-578`）。
- 路由：`packages\api\src\routes.ts`（知识库/评论/回收站/分类/可见性/上传路由）。
- 前端：`apps\web\src\components\{ArticleEditor,ArticleDetail,DocTree,ContentCardFeed,ArchiveView,TrashView,CommentSection}.vue`。

**一致性状态**：主体 **A**；以下为 **D**：
- **评分/点赞/分享、AI 摘要、版本历史**：字段仍在数据模型（`types.ts:35-48` 的 `rating/likes/summary/history/shareToken`），但写路径已删除（`routes.ts` 无 rate/like/share/summary/版本 restore 路由，见门户文档 §16）→ **已下线（字段残留，完善阶段清理）**。
- **归档**：现状为**按更新时间分组的浏览视图（非"隐藏"）**（`ArchiveView.vue:29-38` 按 `updatedAt` 分月展示全部非删除文章）；`status='archived'` 无设置入口。
- **全局搜索**：现状**仅匹配标题 + 标签**（`ContentCardFeed.vue:158-162`），未接入正文/向量检索（扩展后议，见模块 ②）。

**验收要点**：
- 站主可新建/编辑/删除文章，游客只读；非站主点击编辑被拦截（`App.vue:216-218`）。（批量导入 `/api/knowledge/import` 已下线，明细见门户文档 §16）
- 四级可见性在列表/搜索/卡片流/右栏/接口直读中均隔离（游客仅 public）。
- 父级设权限按 self/same/all 三选一递归同步子文件。
- 系统面板可树形批量设可见性（`SystemPanel.vue` → `ArticlePermissions.vue`）。
- 评论：登录可发、作者/admin 可删、不可见文章评论不泄露。
- 封面上传 ≤5MB 且 `/uploads/<file>` 可访问；回收站恢复/清空正常。

---

### 模块 ② 门户布局与全局导航

**概述**：把"控制台"升级为"三栏知识门户"；站主管理收敛到底栏，移动端为底部 Tab。

**关键需求点（来源：`docs\门户布局与功能改造需求文档.md` §4/§5/§15）**：
- 顶栏：品牌 + 全局搜索（Ctrl+K）+ 功能插件 nav + ws 状态 + 头像登录/用户菜单 + 暗色切换。
- 左栏：首页 / 归档 / 回收站 / 新建文章（仅站主）+ 目录树（`DocTree`）+ 站点签名 moto。
- 中栏：知识卡片流（默认首屏）/ 文章详情 / 各功能面板。
- 右栏：数据概览 / 分类 / 标签云 / 热门文章 / 最新评论（widget）。
- 底栏：品牌 + 站主管理（系统/插件/商店/用户，仅 admin）+ 版权。
- 响应式：≥1200 三栏；768–1199 单栏 + 左右抽屉；<768 单栏 + 底部 Tab。
- 全局筛选：右栏"分类/标签云"点击 → 联动中栏卡片流 + 左栏目录，带"✕ 清除"chip。

**实现位置**：`apps\web\src\App.vue`（顶栏 `:366-419`、三栏 `:433-500`、底栏 `:503-516`、移动 Tab `:560-591`）；`apps\web\src\components\{ContentCardFeed,PortalRight,DocTree,SystemPanel}.vue`。

**一致性状态**：布局 **A**。以下为 **D/B**：
- **全局搜索范围（D）**：文档 §14 写"检索知识库标题/标签/正文"，代码现状**仅标题+标签**（`ContentCardFeed.vue:158-162`），且为前端过滤；后端 `knowledge.search()` 混合检索（标题+正文+标签+向量，`service.ts:381-429`）当前只被 Agent 的 `knowledge_search` 工具使用。**扩展后议**。
- **动态日历 widget（B）**、**统计图表 widget（雷达/柱状/趋势）（B）**：未实现（见第五节）。
- **关于我 / 留言板（B）**：未实现；归档库已实现（`ArchiveView.vue`）。
- **moto（D）**：当前为硬编码"以不折腾为目标的折腾"（`App.vue:436`），非站点配置项。
- **`CategoryTree.vue` 仍在用（D）**：门户文档 §14.2 写"已移除"，但 `apps\web\src\components\CategoryTree.vue` 仍存在并被 `PortalRight.vue:4` 引用（右栏"分类"widget）。门户文档 §14.2 原文已于 2026-09 按代码改正。

**验收要点**：
- 三档响应式（≥1200 / 768–1199 / <768）切换正确。
- Ctrl+K 聚焦搜索框；分类/标签点击联动卡片流 + 目录，可一键清除筛选。
- 站主管理（系统/插件/商店/用户）仅 admin 可见且与前台功能分隔。

---

### 模块 ③ 用户体系与权限（账号 / 用户组 / 四级可见性 / 文章与插件可见性）

**概述**：`guest/user/admin` 三角色（角色仅用于管理权限判断）+ **用户组模型**（含父子包含关系），统一驱动文章与插件可见性。

**关键需求点（来源：`docs\用户组与内容可见性权限需求文档.md` §2-6；`AGENTS.md` 数据模型）**：
- 内置组 `guest`/`user`/`admin` 恒存在、不可删；自定义组可增删改 + 设父组（防环）。
- 用户可多组归属，权限取并集；注册用户恒保留 `user` 组；`admin` 恒有全部权限。
- 组的包含关系：`Group.parentId`（上级组包含下级组），`effectiveGroupsOf` 递归展开有效组。
- 文章可见性四级快捷：公开 / 仅登录 / 指定组 / 仅站主（底层 `visibility` + `visibleGroups`）。
- 插件可见性由布尔 `visibility` 迁移为 `visibleGroups`。
- 目录 `⋯` 菜单"🔒 权限设置"支持父子递归同步；系统面板"文章权限批量操作"。

**实现位置**：
- `packages\plugins\account\src\types.ts`（`Group` `:5-14`、`User` `:16-29`、`groupsOf` `:54-58`）。
- `packages\plugins\account\src\service.ts`（`effectiveGroupsOf` `:358-385`、`createGroup`/`deleteGroup`/`setGroupParent` `:270-345`、scrypt 密码 `:11-23`）。
- `packages\plugins\plugin-registry\src\service.ts`（`visibleFor` `:198-202`、旧布尔迁移 `:130-144`）。
- `packages\api\src\routes.ts`（`viewerOf`/`resolveViewer` `:109-116`、`requireAdmin`/`requireUser` `:86-106`、用户/用户组/插件可见性路由）。
- 前端：`apps\web\src\components\{UserManager,EntryPermissionDialog,ArticlePermissions,PluginSettings}.vue`。

**一致性状态**：核心 **A**。以下为 **D/C**：
- **D**：需求文档 §8.2 接口清单未列 `POST /api/groups/:id/parent`（组包含关系，代码已实现，`routes.ts:240-250`）。
- **C**：用户自助改资料 `POST /api/auth/profile`（改用户名/改密码，`routes.ts:158-172`）、用户组父级包含关系前端 UI（`UserManager.vue`）缺文档记录。
- **注册屏蔽（A）**：`POST /api/auth/register` 已删除；站主在"用户管理"手动建号（`POST /api/auth/users`，requireAdmin）。

**验收要点**：
- 游客未登录仅见 public 文章与对 guest 组可见的插件；私密内容在列表/搜索/卡片流/右栏/接口直读均不可见。
- 自定义组父子包含关系生效（上级组成员可见下级组内容）；删除组子组上挂、组内用户回落 user。
- 插件可见性按组即时生效；`admin` 恒可访问全部。
- `/api/auth/users`、`/api/groups`、`/api/plugins` 等管理接口非 admin 返回 403。

---

### 模块 ④ 插件系统（内置 7 插件 / 注册表 / 启停排序 / 商店现状 / 第三方加载）

**概述**：内置插件 + 插件注册表（启停/排序/按组可见）+ 插件商店（硬编码目录）+ 第三方插件包动态加载。

**关键需求点（来源：`docs\插件系统与权限管理体系扩展需求文档.md` §2；门户文档 §8）**：
- 插件注册表：启用/禁用、排序、按用户组可见。
- 插件商店：浏览 / 安装 / 更新 / 卸载 / 评分评论。
- 默认插件不可卸载；`agent` 为内置默认插件。
- 第三方插件包（`plugin.json` + `entry` 模块）动态加载/热装/热卸。

**实现位置**：
- `packages\plugins\plugin-registry\src\service.ts`（`STORE_CATALOG` 硬编码 7 条目 `:25-110`、`visibleFor` `:198-202`、install/uninstall/update/rate `:247-329`）。
- `packages\plugins\plugin-registry\src\loader.ts`（第三方包加载）。
- `packages\plugins\plugin-registry\src\index.ts`（默认插件仅 `agent` `:35-46`）。
- 第三方示例：`plugins\hello\index.ts` + `plugins\hello\plugin.json`。
- 前端：`apps\web\src\components\{PluginStore,PluginSettings,StorePluginPanel}.vue` + `apps\web\src\lib\plugins.ts`。

**一致性状态**：
- **A**：启停/排序/按组可见、商店安装/更新/卸载/评分、第三方包加载（简化落地）。
- **D**：原文档"知识库作为默认插件"已演变为"知识库退出插件库、作为门户默认功能"（`plugin-registry/src/index.ts:34`、`service.ts:16`）。
- **B**：插件导出/导入、真实第三方市场、安全扫描、拖拽实时预览**未实现**（见第六节"完善立项清单"）。
- **C**：第三方包加载规范无文档（仅 `loader.ts` 头注释 + `plugins/hello` 示例）。

**验收要点**：
- 安装/卸载/更新/启停即时生效且刷新前台；`agent` 内置不可卸载。
- 插件按组可见与文章可见性一致。
- 商店评分评论可写；第三方包 `hello` 能动态加载并渲染面板。

---

### 模块 ⑤ AI Agent 多节点与三端互通（会话 / 工具 / 节点 / 远程任务 / 本地工具）

**概述**：服务端 function-calling Agent + 多会话持久化 + 工具（RAG/网页/时间/写库）+ 桌面节点注册/心跳 + 远程派任务（桌面本地工具执行）。

**关键需求点（来源：`docs\AI Agent 多节点与三端互通需求文档.md` §2-3）**：
- 多会话管理（新建/删除/重命名/切换）+ 历史持久化。
- 工具：`knowledge_search`（RAG）、`current_time`、`web_fetch`（网页抓取）、`knowledge_add`（写库，默认关闭）。
- 桌面本地工具：`exec_command`（白名单命令、无 shell）、`read_local_file`/`write_local_file`（仅限 app-data 下 `agent-workspace` 目录）。
- 节点注册/发现/心跳（30s 判定在线）；远程派任务（仅能向自己名下节点派发，`ownerId` 校验）。
- 过程可视化（思考中/工具调用/工具结果/最终回答）。

**实现位置**：
- `packages\plugins\agent\src\index.ts`（工具 `:53-151`、`runStream` `:174-239`、会话 `:243-316`、节点 `:318-364`、任务 `:366-411`）。
- `packages\plugins\agent\src\types.ts`（`AgentSession` `:36-41`、`AgentNode` `:67-74`、`AgentTask` `:84-94`）。
- `packages\api\src\routes.ts`（`:699-932`：会话/节点/任务/SSE/LLM 原始流式）。
- `apps\web\src\lib\agentNode.ts`（本地工具 `:75-116`、本地循环 `:205-237`、启动/心跳/轮询 `:277-295`）。
- `apps\web\src\components\AgentPanel.vue`；桌面原生命令 `apps\web\src-tauri\src\lib.rs`。

**一致性状态**：
- **A**：阶段一~四（会话持久化 / 工具扩展 / 节点注册 / 远程任务 + 本地工具）已落地。
- **D（已知缺口）**：会话**无归属（无 `ownerId`）、无节点绑定（无 `nodeId`）、无鉴权、全局共享**（`agent/src/types.ts:36-41` 仅 id/title/createdAt/updatedAt；`routes.ts:713-868` 会话与 chat-stream 均无 `requireUser`），与"同一账号续聊 / 会话归属"目标不符 → **完善阶段立项**。
- **D**：接口命名偏差——文档 `POST /api/agent/nodes/heartbeat` 实为 `POST /api/agent/nodes/:id/heartbeat`（`routes.ts:757-760`）；文档 `GET /api/agent/tools?nodeId=` 实为无参数 `GET /api/agent/tools`（`routes.ts:699-701`）。
- **B**：显式"计划"阶段、长任务目标+子任务 todo、computer-use、任务过程实时推流未实现（文档自述留作后续）。

**验收要点**：
- 多会话可新建/切换/删除，历史持久化；流式对话展示工具步骤（思考中/调用工具/结果/回答）。
- 桌面节点注册后三端可见，30s 心跳维持在线。
- 移动/Web 向自己名下桌面节点派任务，桌面端本地执行白名单命令/受控文件读写并回传。
- 越界（他人节点、绝对路径/`../`、非白名单命令）被拒。

---

### 模块 ⑥ 数据扩展 store-data（便签 / 日程 / 待办 / 天气 / RSS / MD 剪贴板）

**概述**：`store-data` 服务为商店插件（stats/notes/calendar/todo/weather/rss/mdclip）提供真实数据后端。

**关键需求点（来源：`README.md` 目录结构提"便签/日历/待办/天气/RSS/MD 剪贴板"；门户文档 §3）**：
- 数据统计（知识库条目/已装插件/便签/日程/待办计数）。
- 便签、日程（含 done 勾选）、待办（含 done 勾选）CRUD。
- 天气（open-meteo，无需 key，失败降级）、RSS 抓取解析、MD 剪贴板草稿（保存/复制）。

**实现位置**：
- `packages\plugins\store-data\src\service.ts`（`statistics` `:27-38`、`weather` `:114-136`、`rss` `:139-156`）。
- `packages\api\src\routes.ts`（`/api/plugins-data/*`）。
- 前端：`apps\web\src\components\StorePluginPanel.vue`。

**一致性状态**：**C**（无独立需求文档）；功能与商店条目 **A**。

**验收要点**：七个面板增删改可用；天气/RSS 第三方接口失败有降级；统计计数正确（知识库条目当前为总数、未按可见性过滤——门户文档 §14.3 已注明可选后续）。

---

### 模块 ⑦ 三端与桌面移动（desktop-mobile 摘要 + 实现现状）

**概述**：复用 `apps\web` 前端 + 同一 REST/WS 后端，实现 Web / Tauri 桌面 / Capacitor 移动三端互联。

**关键需求点（来源：`docs\desktop-mobile.md`；`README.md` §三端互联）**：
- API 基地址由 `VITE_API_BASE` 区分：Web 空（相对路径）/ Tauri `http://localhost:3000` / Capacitor 局域网 IP。
- `apiUrl()`/`wsUrl()` 自动适配三端；WS `/ws` 实时联动。
- 桌面端（Tauri 2）内含本地 agent 节点（见模块 ⑤）；移动端（Capacitor）共用前端。

**实现位置**：`apps\web\src\lib\config.ts`（`API_BASE`/`apiUrl`/`wsUrl`）；`apps\web\src\lib\update.ts`（`detectPlatform`/热更新/硬更新）；`apps\web\src-tauri\`（Tauri 壳）；`capacitor.config.ts`。

**一致性状态**：机制 **A**。**D（已修正 2026-09）**：desktop-mobile.md 原写 "JDK 17"，已改为 "JDK 17+（仓库根自带 jdk21/）"。

**验收要点**：Web 同源部署可访问；Tauri/Capacitor 构建注入 `VITE_API_BASE` 后可连后端；更新检测 → 确认 → 进度 → 应用全链路可用。

---

### 模块 ⑧ 部署运维（server-deploy 摘要 + 端口事实）

**概述**：Docker 多阶段构建 + docker-compose 编排 + 数据卷持久化 + 健康检查；JSON/SQLite 双存储。

**关键需求点（来源：`docs\server-deploy.md`；`README.md`；`AGENTS.md`）**：
- 一键部署 `scripts/deploy-server.sh` 或 `docker compose up -d --build`。
- 数据卷 `ikit-data` 持久化（`/app/packages/server/data`）。
- SQLite 大数据量切换（`KB_STORAGE=sqlite`）+ 首次启动自动迁移 JSON。
- 反代（Caddy/Nginx，含 WS/SSE 的 `Upgrade` 头与较长 `proxy_read_timeout`）。

**实现位置**：`docker-compose.yml`（端口映射 `"80:3000"`，含 `healthcheck:` 段）；`Dockerfile`（`EXPOSE 3000`）；`packages\server\src\index.ts`（`.env` 加载 `:18-47`、`KB_STORAGE` 切换 `:64`）；`packages\plugins\knowledge\src\index.ts`（JSON→SQLite 迁移 `:57-100`）。

**一致性状态**：
- **端口事实（已批准修正）**：**本地开发端口 3000；服务器对外端口 80**（`docker-compose.yml` 映射 `80:3000`）。`server-deploy.md`/`README.md` 原文写"访问 `http://<服务器IP>:3000`"属过时，统一为 **80**。
- **D（已修正 2026-09）**：`.env.example` 原缺 `ADMIN_USERNAME/ADMIN_PASSWORD/SMTP_*/APP_URL/PLUGIN_DIR/UPDATE_INSTALLER_URL_*` 等 `server/src/index.ts:70-98` 实际读取的变量——已补齐 `.env.example`（16 键）并在 `docker-compose.yml` 透传。

**验收要点**：`docker compose up -d --build` 后经 80 端口访问；健康检查通过；数据卷重启不丢；SQLite 首次启动自动迁移。

---

## 四、已下线与死字段清单

> 以下为"数据字段仍在、但写路径/交互接口已删除"或"路由已删"的项，来源为代码对照（`packages\api\src\routes.ts` 无对应路由）与门户文档 §16"删除 18 条死路由"。

| 项 | 现状 | 涉及字段/位置 | 处置 |
|---|---|---|---|
| 评分（rating） | 已下线（字段残留） | `KnowledgeEntry.rating`（`knowledge/src/types.ts:40`）；卡片/详情仅展示 `ContentCardFeed.vue:265`、`ArticleDetail.vue:44`，恒为 0/空 | 完善阶段清理 |
| 点赞（likes） | 已下线（字段残留） | `KnowledgeEntry.likes`（`types.ts:42`）同上 | 完善阶段清理 |
| 分享（shareToken） | 已下线（字段残留） | `KnowledgeEntry.shareToken`（`types.ts:48`）；`/api/share/:token` 已删 | 完善阶段清理（门户文档 §7.8 注明"当前未启用"） |
| AI 摘要（summary） | 已下线（字段残留） | `KnowledgeEntry.summary`（`types.ts:38`）；`SummarizeFn` 与 summary 路由已删（门户文档 §16） | 完善阶段清理 |
| 版本历史恢复 | 已下线（仅写入、无读取/恢复 UI） | `KnowledgeEntry.history`（`types.ts:36`，update 时写入 `service.ts:242-253`）；版本 restore 路由已删 | 完善阶段清理 |
| 归档（status='archived'） | 语义漂移 | `KnowledgeEntry.status` 含 `'archived'`（`types.ts:34`）但无设置入口；`ArchiveView.vue` 实为按年月浏览 | 如实描述为"浏览视图" |

> 说明：以上字段删除/恢复需由技术负责人评估关联影响（前端展示、SQLite 列、JSON 兼容）后出方案，见第六节。

---

## 五、规划中（未实现）列表

> 均来自历史/既有需求文档，当前代码未实现，保留为后续规划参考。

| 项 | 来源文档 | 现状 |
|---|---|---|
| 划线 & 批注 | `docs\个人知识库系统 PRD.md` §二.4 | 未实现（早期个人版愿景） |
| 阅读进度记忆 / 阅读时长预估 / 最近浏览 | PRD §二.3/4 | 未实现 |
| 快速记录（灵感闪念）/ 粘贴链接自动抓取 | PRD §二.1 | 未实现 |
| WebDAV 云同步 | PRD §二.5 | 未实现 |
| zip 导入/导出（全量备份） | PRD §二.5 | 未实现（单篇导出也已被删） |
| PWA 离线 | PRD §三/七 | 未实现 |
| 动态日历 widget | 门户文档 §5.4、§6 N11 | 未实现 |
| 统计图表 widget（分类雷达/发布柱状/趋势） | 门户文档 §5.4、§6 N12 | 未实现（当前仅"数据概览"简单条形） |
| 关于我 / 留言板 | 门户文档 §6 N13 | 未实现（归档库已实现） |
| 快捷功能入口（外链式） | 门户文档 §6 N15 | 部分由"功能插件 nav"替代 |
| computer-use（桌面操控：截屏 + 键鼠注入） | `docs\AI Agent 多节点…` §3.2/§8 | 暂缓（用户确认独立后置） |
| 任务过程实时推流 | AI Agent 文档 §3.4 | 未实现（当前前端 3s 轮询 `AgentPanel.vue`） |
| Agent 显式"计划"阶段 / 长任务 todo / 中断续跑 | AI Agent 文档 §3.3 | 未实现（留作后续增强） |

---

## 六、完善立项清单（待技术负责人出方案）

> 以下为已确认需"完善阶段"处理、或需立项评估的缺口，供技术负责人逐项评估关联影响（依赖、共享契约、前端联动、存储迁移）后出方案。

1. **死字段清理**：`rating` / `likes` / `shareToken` / `summary` / `history`（版本恢复）——评估是否移除数据模型字段、前端展示、SQLite 列与 JSON 兼容（见第四节）。
2. **Agent 会话归属 + 鉴权**：会话补 `ownerId`（按账号隔离），会话/消息/chat-stream 路由补 `requireUser`；评估是否补 `nodeId` 节点绑定（见模块 ⑤）。
3. **插件生态类（未实现愿景）**：插件导出/导入、真实第三方市场、安全扫描、拖拽排序实时预览——是否纳入后续迭代（见模块 ④）。
4. ✅ **门户搜索扩展（已决策 2026-09：暂不实施，保留可选）**：全局搜索是否接入后端 `knowledge.search()`（标题+正文+标签+向量）以兑现"搜正文"（见模块 ②，扩展后议）。
5. ✅ **归档语义定稿（已决策 2026-09：按"浏览视图"描述，不恢复隐藏语义）**：按"浏览视图"还是恢复"隐藏"语义（见模块 ①）。
6. ✅ **默认修正项落地（已完成 2026-09）**：README 目录结构与 API 表重写、门户 §14.2 CategoryTree 误述修正、端口统一（本地 3000 / 服务器 80）、`.env.example` 补全 + docker-compose 透传；`package.json` description 乱码核查为无问题。

---

## 七、验收对照说明

- 各模块的"验收要点"即为可操作验收点，验收时按模块逐条核对（对照代码与接口，不依赖文档文字）。
- 一致性状态 A 的模块，验收=回归验证（构建 + 接口冒烟）；B/D 项不在本轮验收范围（见第五、六节）；C 项需先补文档再验收。
- 构建/类型检查基线：`pnpm -r --if-present build` + `tsc --noEmit` 通过。
- 验收完成后，结论回写本文（更新对应模块的"一致性状态"），并同步 `AGENTS.md` 数据模型与相关原文档。

---

*（本文档为现状事实基准 v1.1；后续任何需求新增/变更请先更新本文，再按 `AGENTS.md` 同步各原文档。）*
